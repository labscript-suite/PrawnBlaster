name: prawnblaster

services:
  buildfirmware:
    build:
      dockerfile: build/docker/Dockerfile
    command: /bin/bash -c 'cmake .. && make'
    volumes:
      - .:/prawnblaster
    working_dir: /prawnblaster/build
    init: true
