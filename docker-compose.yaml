name: prawnblaster

services:
  buildfirmware:
    build:
      dockerfile: docker/Dockerfile
    command: /bin/bash -c 'cp /pico/pico-sdk/external/pico_sdk_import.cmake /prawnblaster/ && cmake .. && make'
    volumes:
      - .:/prawnblaster
    working_dir: /prawnblaster/build
    init: true
